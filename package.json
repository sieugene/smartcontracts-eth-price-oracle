{
  "name": "EthPriceOracle",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:oracle": "cd ./oracle && truffle compile",
    "build:caller": "cd ./caller && truffle compile",
    "build": "npm run build:oracle && npm run build:caller",
    "loom:install": "curl https://raw.githubusercontent.com/loomnetwork/loom-sdk-documentation/master/scripts/get_loom.sh | bash",
    "loom:init": "./loom init",
    "loom:generate": "./loom genkey -a public_key -k private_key"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@openzeppelin/contracts": "^4.1.0",
    "axios": "^0.20.0",
    "bn.js": "^5.2.0",
    "node-gyp": "^8.1.0",
    "openzeppelin-solidity": "^4.1.0",
    "truffle": "^5.3.10"
  }
}
